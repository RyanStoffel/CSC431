# Assignment 10: Multi-Factor Authentication System

A Java program that implements multi-factor authentication. Users register with a username, password, and a second factor (OTP or security question). Passwords are hashed with SHA-256 and a random salt. Accounts lock after 3 failed login attempts.

## How to Run

```
java MFA.java
```